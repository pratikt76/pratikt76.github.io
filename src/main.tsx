import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import PratikMinimalPortfolio from './PratikMinimalPortfolio';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PratikMinimalPortfolio />
  </React.StrictMode>
);
